export const ACTION_EMAIL = (email) => ({ type: 'ACTION_EMAIL', email });
export const ACTION_CURRENCIES = (value) => ({ type: 'ACTION_CURRENCIES', value });
export const ACTION_EXPENSES = (value) => ({ type: 'ACTION_EXPENSES', value });
